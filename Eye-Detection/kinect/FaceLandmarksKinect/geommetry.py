import numpy as np
import math

kinect_position = (x, y, z)
theta_k
R_kinect = np.array([[cos(theta_k), sin(theta_k), 0], [-sin(theta_k), cos(theta_k), 0], [0, 0, 1]])

theta_node_1 = 3*313*pi/(180*16)
theta_node_2 = 313*pi/(180*4)
